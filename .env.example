# Formspree form id. Without it the contact form says it is not connected and
# points the visitor at the email address instead.
# Get it from https://formspree.io (the bit after /f/ in your form endpoint).
NEXT_PUBLIC_FORMSPREE_ID=
